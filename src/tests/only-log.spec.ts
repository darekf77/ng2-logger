import { _ } from 'tnp-core';
import { describe, before, beforeEach, it } from 'mocha';
import { expect } from 'chai';

describe('DB test', () => {

  it('should be empty db at begin', () => {

    expect(true).to.be.true;
  });


  it('should be empty db at end', () => {

    expect(true).to.be.true;
  });


});

